export const environment = {
  production: false,
  apiUrl: 'http://localhost:3001', // Go backend server port
  wsUrl: 'ws://localhost:3001', // Go backend WebSocket URL (if needed in the future)
  legacyApiUrl: 'http://localhost:3001', // Keep reference to old API if needed
};
